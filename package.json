{
  "name": "bisect-sorted-set",
  "version": "1.0.1",
  "description": "An in memory sorted set that uses binary serach on numeric indexes to find values next to each other",
  "scripts": {
    "test": "prettier-standard-formatter *.js && tape test.js"
  },
  "dependencies": {
    "sorted-array-functions": "^1.0.0"
  },
  "devDependencies": {
    "prettier-standard-formatter": "^0.222222222222222.333333333333333",
    "tape": "^4.6.3"
  }
}
